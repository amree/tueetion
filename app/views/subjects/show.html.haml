.breadcrumb-container
  %ol.breadcrumb
    %li= link_to "Home", root_path
    %li.active Setup
    %li= link_to "Subjects", subjects_path
    %li.active= @subject.name

= render 'layouts/flash_messages'

.row
  .col-md-3
    = render 'layouts/setup_sidebar'

  .col-md-9
    .panel.panel-borderless
      .panel-body
        .section-title
          .btn-group.pull-right
            = link_to subjects_path, class: "btn btn-default" do
              %i.fa.fa-chevron-left.fa-fw
              List
            %button.btn.btn-default.dropdown-toggle{ :"data-toggle" => "dropdown", type: "button" }
              Actions
              %span.caret
            %ul.dropdown-menu{ role: "menu" }
              %li
                = link_to edit_subject_path(@subject) do
                  %i.fa.fa-pencil.fa-fw
                  Edit
              %li
                = link_to @subject, method: :delete, data: { confirm: "Are you sure you want to delete this subject?" } do
                  %i.fa.fa-trash-o.fa-fw
                  Delete

          %h2
            Subjects

        %dl.dl-horizontal
          %dt
            Name
          %dd
            = @subject.name
          %dt
            Status
          %dd
            = status_label(@subject.is_active)
          %dt
            Created at:
          %dd
            = l @subject.created_at, format: :default
          %dt
            Updated at:
          %dd
            = l @subject.updated_at, format: :default

