.section-title
  = link_to "New Subject", new_combination_fee_combination_item_fee_path(combination_fee), class: "btn btn-primary pull-right"
  %h2 Combination Fee Subjects

- if combination_item_fees.size > 0
  %table.table.table-hover.table-text-middle
    %thead
      %tr
        %th #
        %th Subject
        %th Updated at
        %th Added at
        %th
    %tbody
    - i = 0
    - combination_item_fees.each do |combination_item_fee|
      %tr
        %td= i = i + 1
        %td= combination_item_fee.subject.name
        %td= l combination_item_fee.updated_at, format: :default
        %td= l combination_item_fee.created_at, format: :default
        %td.text-right
          = link_to edit_combination_fee_combination_item_fee_path(combination_fee, combination_item_fee), class: "btn btn-primary" do
            %i.fa.fa-pencil.fa-fw
          = link_to [combination_fee, combination_item_fee], :method => :delete, :data => { :confirm => 'Are you sure you want to remove this subject?' }, class: "btn btn-danger" do
            %i.fa.fa-trash-o.fa-fw

- else
  No subject to be displayed.
