= search_form_for q, html: { class: "form-inline" } do |f|
  .form-group
    = f.text_field :full_number_cont, placeholder: "Bill #", class: "form-control"

  .form-group.right-form-buffer
    = f.text_field :student_first_name_or_student_last_name_cont, placeholder: "First or Last Name", class: "form-control"

  .checkbox
    %label.right-form-buffer
      = f.check_box :is_paid_false
      Unpaid

    %label.right-form-buffer
      = f.check_box :is_overdue_true
      Overdue

  = f.submit class: "btn btn-default"
