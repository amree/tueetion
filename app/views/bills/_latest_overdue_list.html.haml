- if bills.size > 0
  %table.table.table-hover.table-text-middle
    %thead
      %tr
        %th Bill #
        %th Period
        %th.text-right
          Amounts (RM)
    %tbody
      - i = 0
      - bills.each do |bill|
        %tr
          %td= bill.full_number
          %td
            = I18n.t("date.month_names")[bill.month]
            = bill.year
          %td.text-right
            = number_with_precision(bill.total_amount, precision: 2)

- else
  No latest overdue bills.

