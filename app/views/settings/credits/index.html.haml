.breadcrumb-container
  %ol.breadcrumb
    %li= link_to "Home", root_path
    %li Settings
    %li.active= link_to "SMS Credits", settings_credits_path

.row
  .col-md-3
    = render 'layouts/settings_sidebar'

  .col-md-9
    .panel.panel-borderless
      .panel-body
        .section-title
          %h2
            Credits

        - if @credits.size > 0
          %table.table.table-hover.table-text-middle
            %thead
              %th #
              %th Bought At
              %th Usage
              %th
            %tbody
              - i = 0
              - @credits.each do |credit|
                %tr
                  %td
                    = i += 1
                  %td
                    = l credit.transactions.first.paid_at
                  %td
                    = credit.used
                    \/
                    = credit.amount
                  %td.text-right
                    = link_to settings_credit_path(credit), class: "btn btn-default" do
                      %i.fa.fa-eye.fa-fw

        - else
          No SMS credit purchases has been recorded.
