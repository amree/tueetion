- if branches.size > 0
  %table.table.table-hover
    %thead
      %th Name
      %th
    %tbody
      - branches.each do |branch|
        %tr
          %td.text-middle
            = branch.name
          %td.text-right
            -# = link_to "Edit", edit_center_branch_path(center, branch), class: "btn btn-primary"
            = link_to edit_center_branch_path(center, branch), class: "btn btn-primary" do
              %i.fa.fa-pencil.fa-fw
              Edit
- else
  You haven't added any branch yet.
